# 用友产业案例教学课程定制系统 — 重构计划

## Context

将旧项目（Flask + 单HTML页面 + Excel内存数据）重构为现代全栈架构：Vue3+TS+Tailwind CSS 前端 + Python3 FastAPI+SQLite 后端。旧系统仅有5步向导功能，无数据库、无管理后台、无统计分析。重构需新增管理后台（数据源管理 + 访问统计可视化）。

---

## 一、项目目录结构

```
yonyou-case/
├── config.yaml                      # 全局统一配置文件（LLM、管理员、服务器、前端等）
├── config.yaml.example              # 配置模板
├── backend/
│   ├── app/
│   │   ├── main.py                  # FastAPI 应用入口、CORS、lifespan
│   │   ├── config.py                # 读取根目录 config.yaml
│   │   ├── database.py              # SQLite + SQLAlchemy 连接
│   │   ├── models/
│   │   │   ├── enterprise.py        # Enterprise 企业数据表
│   │   │   ├── admin.py             # AdminUser 管理员表
│   │   │   └── analytics.py         # VisitLog 访问日志表
│   │   ├── schemas/                 # Pydantic 请求/响应模型
│   │   ├── routers/
│   │   │   ├── wizard.py            # 向导公开接口（8个）
│   │   │   ├── admin_auth.py        # 管理员登录
│   │   │   ├── admin_enterprises.py # 企业CRUD
│   │   │   ├── admin_import.py      # Excel导入
│   │   │   └── admin_analytics.py   # 统计分析接口（5个）
│   │   ├── services/
│   │   │   ├── enterprise_service.py   # 企业数据查询
│   │   │   ├── generate_service.py     # OpenAI兼容API + 模板生成
│   │   │   ├── auth_service.py         # JWT + bcrypt
│   │   │   ├── import_service.py       # Excel解析
│   │   │   └── analytics_service.py    # 访问日志聚合
│   │   └── middleware/
│   │       └── analytics_middleware.py # 自动记录访问日志
│   ├── data/data.xlsx               # 种子数据
│   ├── requirements.txt
│   └── seed.py                      # 首次运行导入Excel到SQLite
├── frontend/
│   ├── src/
│   │   ├── api/                     # http.ts + wizard.ts + admin.ts
│   │   ├── types/index.ts           # 所有TS接口定义
│   │   ├── composables/
│   │   │   ├── useWizard.ts         # 向导状态（步骤、选项、级联）
│   │   │   └── useAuth.ts           # 认证状态
│   │   ├── views/                   # 页面级组件
│   │   ├── components/
│   │   │   ├── wizard/              # 5步向导组件
│   │   │   ├── admin/               # 管理后台组件
│   │   │   └── shared/              # 复用组件（OptionButton, SvgTooltip, MarkdownRenderer）
│   │   └── router/index.ts
│   ├── vite.config.ts
│   └── package.json
└── old/                             # 原项目（保留不动）
```

---

## 二、全局配置文件 config.yaml

项目根目录统一配置，前后端共用。后端直接读取 YAML 文件，前端通过 `/api/config` 接口获取非敏感配置。

```yaml
# config.yaml
server:
  host: "0.0.0.0"
  port: 8000
  cors_origins: ["http://localhost:5173"]

database:
  url: "sqlite:///./data/app.db"

admin:
  username: "admin"
  password: "changeme"      # 首次启动后建议修改

llm:
  api_key: "sk-xxx"
  api_base_url: "https://api.openai.com/v1"   # 兼容OpenAI格式的任意API地址
  model: "gpt-4o"                              # 任意兼容模型名
  temperature: 0.7
  max_tokens: 2000
  timeout: 60

pricing:
  rate_per_hour: 2000       # 每课时单价（元）

frontend:
  title: "用友产业案例教学项目课程定制系统"
```

**配置读取方式**：
- **后端**：`config.py` 使用 `pyyaml` 读取根目录 `config.yaml`，提供全局 `settings` 对象
- **前端**：GET `/api/config` 返回 `frontend` 配置段 + 后端API地址（前端无需单独配置文件）
- **敏感信息**（api_key、password）仅后端使用，不暴露给前端

---

## 三、数据库设计（SQLite，3张表）

### enterprises
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| customer_name | TEXT NOT NULL | 客户名称 |
| province | TEXT NOT NULL | 客户所在省 |
| city | TEXT NOT NULL | 客户所在市 |
| industry | TEXT NOT NULL | 标准行业 |
| company_intro | TEXT | 企业简介 |
| yonyou_content | TEXT | 用友建设内容 |
| created_at / updated_at | TIMESTAMP | 时间戳 |

索引：`industry`, `province`, `(industry, province, customer_name)`

### admin_users — 管理员
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| username | TEXT UNIQUE | 用户名 |
| password_hash | TEXT | bcrypt哈希 |

### visit_logs — 访问日志（统计分析数据源）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| endpoint | TEXT | 请求路径 |
| method | TEXT | HTTP方法 |
| ip_address | TEXT | IP |
| user_agent | TEXT | 浏览器 |
| industry | TEXT | 行业（从请求体提取） |
| region | TEXT | 省份 |
| enterprise | TEXT | 企业 |
| major | TEXT | 专业 |
| hour | TEXT | 课时 |
| request_timestamp | TIMESTAMP | 访问时间 |

索引：`timestamp`, `industry`, `region`

---

## 四、API 设计

### 公开接口（无需认证，兼容旧API）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/majors` | 返回3个专业方向 |
| GET | `/api/industries` | 返回10个行业 |
| GET | `/api/config` | 返回前端配置（标题等非敏感信息） |
| POST | `/api/regions` | 按行业筛选省份 |
| POST | `/api/enterprises` | 按行业+省份筛选企业 |
| POST | `/api/enterprise-info` | 获取企业详情 |
| GET | `/api/hours` | 返回4个课时选项 |
| POST | `/api/generate` | 生成案例（AI大模型或模板），价格从 config.yaml 读取 |

### 管理员认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/login` | JWT登录 |

### 企业管理（需JWT）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/enterprises` | 分页列表+筛选 |
| POST | `/api/admin/enterprises` | 新增 |
| PUT | `/api/admin/enterprises/{id}` | 编辑 |
| DELETE | `/api/admin/enterprises/{id}` | 删除 |
| POST | `/api/admin/enterprises/import` | Excel批量导入 |

### 统计分析（需JWT）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/analytics/summary` | 总访问量、企业数、今日/本周 |
| GET | `/api/admin/analytics/visits` | 时间维度访问趋势 |
| GET | `/api/admin/analytics/provinces` | 省份分布 |
| GET | `/api/admin/analytics/case-frequency` | 案例使用频次Top20 |
| GET | `/api/admin/analytics/industries` | 行业分布 |

---

## 五、前端组件树与数据流

### 公共向导页（/）
```
WizardView
├── StepProgress          — 5步进度条
├── StepMajor (v-if=step1) — @select(major)
├── StepIndustry (v-if=step2) — @select(industry) → 调/api/regions
├── StepRegion (v-if=step3) — @select(region) → 调/api/enterprises
├── StepEnterprise (v-if=step4) — @select(enterprise) → 调/api/enterprise-info
│   └── EnterpriseInfoPanel — 右侧面板，显示企业详情
├── StepHour (v-if=step5) — @select(hour)
└── 提交按钮 → POST /api/generate → 跳转ResultView

ResultView
├── MarkdownRenderer     — marked.js渲染
└── 价格 + 成果物清单 + 返回按钮
```

**状态管理**：`useWizard.ts` composable 维护一个响应式状态对象，包含 currentStep、各选项值、级联数据。核心逻辑：
- init() 并行加载 majors/industries/hours
- 选择行业 → 重置下游选择 + 触发 regions 请求
- 选择省份 → 重置企业 + 触发 enterprises 请求
- 选择企业 → 触发 enterprise-info 请求
- 所有5项选中 → 启用提交按钮

### 管理后台（/admin/*）
```
AdminLayout
├── AdminSidebar         — 导航：企业管理 / 统计面板
├── EnterpriseTable (/admin/enterprises)
│   ├── 筛选栏：行业 + 省份 + 关键词
│   ├── 分页表格：编辑/删除
│   ├── EnterpriseForm (弹窗) — 新增/编辑
│   └── ImportDialog (弹窗) — Excel上传
└── AnalyticsDashboard (/admin/analytics)
    ├── DashboardStats   — 4张统计卡片
    ├── VisitTimeline    — SVG折线图（时间趋势）
    ├── ProvinceBarChart — SVG柱状图（省份分布）
    ├── CaseFrequencyChart — SVG水平条形图（案例频次）
    └── IndustryPieChart — SVG饼图（行业分布）
```

---

## 六、SVG 图表实现方案（自定义SVG，无第三方图表库）

每个图表组件：固定 viewBox，数据→像素坐标线性映射，hover 透明覆盖层 `<rect>` 触发 tooltip。

| 图表 | SVG类型 | viewBox | tooltip方案 |
|------|---------|---------|------------|
| 访问趋势折线图 | `<path>` + `<circle>` | 0 0 700 300 | hover点显示日期+次数 |
| 省份柱状图 | `<rect>` | 0 0 700 300 | hover柱显示省份+数量 |
| 案例频次条形图 | `<rect>` 水平 | 0 0 700 400 | hover条显示企业+次数+行业 |
| 行业饼图 | `<path>` (arc) | 0 0 400 400 | hover扇区显示行业+百分比 |

Tooltip 实现：SvgTooltip 组件，接收 x/y/visible/content，用 absolute 定位 div。

---

## 七、关键技术决策

| 决策 | 方案 | 原因 |
|------|------|------|
| 认证 | JWT (HS256, 24h) | 无状态，适合前后端分离 |
| 数据收集 | FastAPI中间件拦截请求 | 自动记录，零代码侵入 |
| Excel导入 | openpyxl解析 + 中文列名映射 | 兼容旧Excel格式 |
| 图表 | 自定义SVG | 仅4种图表，无需ECharts(800KB) |
| 状态管理 | Vue composable | 项目规模小，无需Pinia |
| 管理员种子 | config.yaml配置 + 启动时自动创建 | 首次运行零配置 |
| 配置管理 | 根目录 config.yaml 统一配置 | 前后端共用单一配置源，后端读YAML，前端通过API获取 |
| AI大模型 | 兼容OpenAI API格式 | 可配置API地址、模型名、API Key，支持任意OpenAI兼容模型 |

---

## 八、任务拆分与调度

### Phase 1：脚手架（3个智能体并行）

**Backend Agent**
- B1: 创建目录 + requirements.txt + config.py + database.py
  - config.py: 读取项目根目录 `config.yaml`（pyyaml），提供全局 `settings` 对象
  - 提供 GET `/api/config` 接口，返回前端所需的非敏感配置
  - config.yaml.example 包含所有配置项的示例值和注释
- B2: 3个ORM模型 + seed.py（导入data.xlsx到SQLite）
- B3: auth_service + POST /api/admin/login

**Frontend Agent**
- F1: Vite + Vue3 + TS + Tailwind 项目初始化
- F2: types/index.ts + api/http.ts + api/wizard.ts + api/admin.ts
- F3: useWizard.ts + useAuth.ts composables
- F4: router 配置 + 导航守卫

**UX Agent**
- U1: 统计面板布局规范（4卡片+2x2图表网格）
- U2: 4种SVG图表组件规范（viewBox、坐标映射、tooltip策略）

### Phase 2：核心功能（Backend先行，Frontend跟进）

**Backend Agent**
- B4: 8个公开向导接口 + enterprise_service + generate_service（含OpenAI兼容大模型调用）
  - generate_service: 从 config.yaml 读取 `llm.api_base_url`、`llm.api_key`、`llm.model`，调用 `{api_base_url}/chat/completions`
  - 支持配置任意兼容OpenAI API的模型（DeepSeek、通义千问、智谱、Moonshot、GPT等）
  - API不可用时回退到本地模板生成（与旧逻辑一致）

**Frontend Agent**（B4完成后接入真实API）
- F5: WizardView + 5个Step组件 + EnterpriseInfoPanel + ResultView

### Phase 3：管理功能（并行）

**Backend Agent**
- B5: analytics中间件 + 5个统计接口 + analytics_service
- B6: 企业CRUD + Excel导入

**Frontend Agent**
- F6: LoginView + AdminLayout + AdminSidebar + EnterpriseTable + EnterpriseForm + ImportDialog

### Phase 4：统计可视化（Frontend + UX）

**Frontend Agent**
- F7: DashboardStats + VisitTimeline + ProvinceBarChart + CaseFrequencyChart + IndustryPieChart（参考U2规范实现SVG）

**Backend Agent**
- B7: main.py串联所有路由 + CORS + lifespan + 测试

---

## 九、验证方案

1. **后端**：启动 `uvicorn app.main:app`，用 curl 测试所有公开接口，确认响应格式兼容旧API
2. **前端**：`npm run dev` 启动开发服务器，走完5步向导流程，确认级联选择、生成结果正常
3. **管理后台**：登录后测试企业CRUD、Excel导入、统计面板4个图表渲染和tooltip
4. **数据一致性**：确认 seed.py 导入后企业数量与Excel行数一致
5. **分析数据**：操作向导后刷新管理面板，确认 visit_logs 有数据、图表有内容
